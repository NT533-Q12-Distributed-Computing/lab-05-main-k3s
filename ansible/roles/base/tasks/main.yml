---
# 🧰 Base system setup for all nodes
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install essential packages
  apt:
    name: ['curl', 'ca-certificates', 'gnupg', 'jq']
    state: present
